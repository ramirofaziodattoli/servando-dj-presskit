export type Event = {
  name: string;
  date: string;
  location: string;
  link: string;
};

export type TechnicalRiderElement = {
  title: string;
  name: string;
  img: string;
  detail?: string;
};
